apiVersion: resource.k8s.io/v1
kind: ResourceClaimTemplate
metadata:
  name: webhook-test
spec:
  spec:
    devices:
      requests:
      - name: ts-gpu
        exactly:
          deviceClassName: gpu.dra-example-driver.sigs.k8s.io
      - name: sp-gpu
        exactly:
          deviceClassName: gpu.dra-example-driver.sigs.k8s.io
      config:
      - requests: ["ts-gpu"]
        opaque:
          driver: gpu.dra-example-driver.sigs.k8s.io
          parameters:
            apiVersion: gpu.resource.dra-example-driver.sigs.k8s.io/v1alpha1
            kind: GpuConfig
            sharing:
              strategy: TimeSlicing
              timeSlicingConfig:
                interval: InvalidInterval
